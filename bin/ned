#!/usr/bin/env node

const program = require('commander')

program
  .command('dev', 'run app in dev mode')
  .command('lint', 'lint app for JavaScript Standard Style compliance')
  .command('test', 'run tests')
  .command('transpile', 'transpile application')
  .parse(process.argv)
